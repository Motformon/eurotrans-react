import { DATA_WAY } from "../actions/actionTypes";


const initialState = {
	
}

export default function booking(state = initialState, action) {
	switch (action.type) {
    case DATA_WAY:
			return {
				
			}

    default:
      return state
  }
}
